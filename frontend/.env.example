# Public API location used by the browser. This is configuration, not a secret.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Never put API keys or private credentials in VITE_* variables.
# Vite includes every VITE_* value in the public browser bundle.
